package application;

public class Steuerung {
	
	public static String nutzerID;

	public static void main(String[] args) {
		/**
		 *  ToDo: 
		 *   - frage Nutzer nach seiner ID (extra button fuer keine id)
		 *   - prüfe, ob Speicherdatei mit nutzerid bereits vorhanden --> wenn ja laden
		 *   - lade sonst xml-eingabe und generiere neue nutzerid --> wie? anz nutzerdat zaehlen, dann 
		 *     zufallszahl zw 0 und 1 addieren, dann hash anwenden
		 *   - variablen zuordnen
		 *   - fenster starten 
		 *     (statisch: nutzerid-anzeige, textfeld (mit scrolleiste!?), textgroesse, ok-button, 
		 *     fortschrittsbalken bzw prozentangabe; 
		 *     dynamisch: auswahl"knöpfe" und labelname-feld)
		 *   - timer starten (eine Stunde) --> auch im fenster anzeigen
		 *   - text auswählen und angaben in feld eintragen
		 *   - wdh bis texte alle oder fenster geschlossen
		 *   - texte alle --> anzeige, dass fertig; dann ausgabe-xml schreiben (mit nutzerid als titel)
		 *   - nutzer schließt fenster --> kleines fenster mit fortschritt; dann nutzerid.datei speichern
		 *   - programm beenden
		 */

	}

}
